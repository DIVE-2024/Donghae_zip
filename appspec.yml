version: 0.0
os: linux

files:
  - source: Donghae_zip-0.0.1-SNAPSHOT.jar
    destination: /home/ec2-user/backend-app

hooks:
  BeforeInstall:
    - location: scripts/stop_server.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
