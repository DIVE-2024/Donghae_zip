version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo Installing dependencies...
  build:
    commands:
      - echo Building the project...
      - ./gradlew build -x test
artifacts:
  files:
    - build/libs/Donghae_zip-0.0.1-SNAPSHOT.jar